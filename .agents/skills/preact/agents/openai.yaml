interface:
  display_name: "Preact"
  short_description: "Repo-specific patterns and workflows for preact"
  default_prompt: "Use the preact repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true